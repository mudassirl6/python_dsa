from array import *
import numpy as np

a1 = array('i',[1,2,3,4,5,6,7,8])
for element in a1:
    print(element)
    
arr = np.array([1,2,3,4,5,6,7])
print(arr)
#Functions 
#append,count,extenmd,index,insert,pop(),remove()



# FOR CREATION OF VIRTUAL ENVIRONMENT IN PYTHON

# conda create -n myenv python=3.8 -> version of python

# for activating the conda environment

# kernel is installed for the jupyter notebook in the conda environment
# what is the use of the kernel in the jupyter notebook
# conda install -c anaconda ipykernel
# python -m ipykernel install --user --name=myenv
# kernel is used to run the code in the jupyter notebook
# Kernel is a program that runs and examines the code in the notebook
# kernel is responsible for running the code in the notebook
#  what is semantics in the programming language
# semantics is the meaning of the code in the programming language